# Hadolint config (https://github.com/hadolint/hadolint)

ignored:
  # DL3008 — "Pin versions in apt get install"
  # Pinning apt package versions breaks the image every time Debian
  # rotates a package (weekly). The reproducibility gain is illusory
  # since debian:bookworm-slim is already a floating tag. For bit-for-bit
  # reproducibility, pin the base image to a digest instead.
  - DL3008

failure-threshold: warning
